# Optional. Without it, Biscuit uses on-device fallback copy.
# The ledger, allocator, graph, and receipts do not need this key.
# https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

NEXT_PUBLIC_APP_URL=http://localhost:3000
